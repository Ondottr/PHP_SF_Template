<h2>Rendered by {{ $engine }}</h2>
<p>This fragment is a Blade (BladeOne) template rendered from a PHP_SF controller, wrapped in the app header/footer layout.</p>
<p>Page title from <code>pageTitle()</code>: {{ pageTitle() }}</p>
<p>CSRF hidden input from <code>&#64;csrf</code>: <span class="blade-csrf">@csrf</span></p>
